# Аудит зависимостей перед редизайном — Эпик #44, подзадача #45

**Дата:** 2026-05-07
**Тип:** read-only, никакой код не меняется

## TL;DR

- **AI-помощник** в коде уже отсутствует — есть только колонка `users.has_ai_addon`. Подзадача #50 = drop колонки.
- **Маркетинговые упоминания пазлов и тарифы (399/799/1199 ₽) в blade-views отсутствуют** — лендинг уже чистый.
- **Реальная работа = выпил моделей + БД-миграции + правка ~10 контроллеров/команд.**
- **Trip-wires проверены и автономны:** мини-игры, рефералы, parent app не пересекаются с выпиливаемыми кусками.
- **Один открытый вопрос:** Telegram Stars (`StarTransaction`) — это не геймификация, а платёжная регистрация в TG Bot API. Решить отдельно (см. секцию «Открытые вопросы»).

---

## 1. Пазлы (#46)

### Модели под удаление
| Модель | Файл |
|---|---|
| `Task` | `app/Models/Task.php` |
| `TaskStep` | `app/Models/TaskStep.php` |
| `StepBlock` | `app/Models/StepBlock.php` |
| `StepBlockSelection` | `app/Models/StepBlockSelection.php` |
| `PuzzleTemplate` | `app/Models/PuzzleTemplate.php` |
| `Attempt` (legacy puzzle) | `app/Models/Attempt.php` |
| `AttemptStep` | `app/Models/AttemptStep.php` |

> ⚠️ `Attempt` (legacy) ≠ `OgeAttempt` (активная). Drop только legacy.

### Точки правки в коде
| Файл | Что сделать |
|---|---|
| `app/Http/Controllers/Api/TaskController.php` | использует `Attempt::create()` — переписать или удалить роут |
| `app/Console/Commands/GenerateOgeTasks.php` | создаёт `TaskStep`/`StepBlock` — удалить или переписать без пазлов |
| `app/Console/Commands/GeneratePuzzles.php` | вся команда обслуживает пазлы — удалить целиком |

### БД (миграции drop + backup перед drop)
| Таблица | Rows на проде | Источник |
|---|---|---|
| `tasks` | 6163 | `2026_01_02_000003_create_tasks_tables.php` |
| `task_steps` | 6669 | то же |
| `step_blocks` | **27394** | то же |
| `puzzle_templates` | ? | то же |
| `task_skills` | ? | то же — **переоценить!** есть в скилл-системе, но `skills` сама остаётся |
| `attempts` (legacy) | ? | `2026_01_02_000004_create_attempts_tables.php` |
| `attempt_steps` | ? | то же |
| `step_block_selections` | ? | то же |

> ⚠️ **task_skills** — связка задач со скиллами. Скиллы остаются (#44 их не трогает). Уточнить: связка живёт через `tasks.id`, который мы дропаем. Решение: дропать вместе с `tasks`.

> ⚠️ **topics** в этой же миграции — это **НЕ выпиливаемые задания.** Не дропать! (Проверить, что используется только для legacy пазлов.)

---

## 2. Геймификация (#47)

### Модели под удаление
| Модель | Файл |
|---|---|
| `Badge` | `app/Models/Badge.php` |
| `UserBadge` | `app/Models/UserBadge.php` |
| `League` | `app/Models/League.php` |
| `LeagueParticipant` | `app/Models/LeagueParticipant.php` |
| `Duel` | `app/Models/Duel.php` |
| `UserStreak` | `app/Models/UserStreak.php` |
| `UserDailyStat` | `app/Models/UserDailyStat.php` |
| `UserGift` | `app/Models/UserGift.php` |

### Точки правки
| Файл | Что вырезать |
|---|---|
| `app/Models/User.php` | релейшены `streak()`, `badges()`, `leagueParticipations()`, `duelsAsChallenger()`, `duelsAsOpponent()`, `userDailyStats()` |
| `app/Models/Attempt.php` | `belongsTo(Duel::class)` — но Attempt и так выпиливается с пазлами |
| `app/Http/Controllers/Api/AuthController.php` | `current_streak`, `badges_count` в ответе профиля |
| `app/Http/Controllers/Pwa/StudentController.php` | `UserGift::query()` (lines 99, 187, 251) |
| `app/Http/Controllers/Pwa/VprController.php` | `UserGift::where(...)` (line 127) |
| `app/Console/Commands/DeleteTelegramUserCommand.php` | `UserGift::whereIn(...)` cleanup |
| `app/Console/Commands/PremiumGiftCommand.php` | использует `UserGift::create` — выпилить целиком |
| `resources/views/dashboard.blade.php` | `badges_count` (line 54) |
| `resources/views/layouts/dashboard.blade.php` | `current_streak` (line 116) |

### БД
Миграция `2026_01_02_000005_create_gamification_tables.php` создаёт **10 таблиц**:
- `user_streaks`, `badges`, `user_badges`, `leagues`, `league_participants`
- `duels`, `duel_tasks`
- `challenges`, `challenge_teams`, `challenge_team_members` (командные челленджи — давно не упоминались, но таблицы есть)

Дополнительно:
- `user_gifts` (отдельная миграция `2026_03_15_400000_create_user_gifts_table.php`)

### Колонки на `users` под выпил
В миграции `2026_01_02_000001_update_users_table.php`:
- `has_ai_addon` (это для AI-помощника, см. #50)
- `trial_ends_at`
- `subscription_plan`
- `subscription_ends_at`

В миграции `2026_03_15_100000_add_telegram_premium_fields.php`:
- `tg_premium_until`
- `tg_trial_used`
- `star_balance` (по drop в той же миграции — уже не существует на проде? проверить!)

---

## 3. Мини-игры — TRIP-WIRE (НЕ ТРОГАТЬ) (#48)

| Файл | Статус |
|---|---|
| `app/Services/PracticeGameService.php` | KEEP |
| `app/Services/PracticeGraphRenderer.php` | KEEP |
| `app/Services/PracticeLeaderboardService.php` | KEEP |
| `app/Http/Controllers/Pwa/PracticeController.php` | KEEP |
| `practice_game_runs` (54 строки) | KEEP |

**Pure isolation:** PracticeLeaderboardService запрашивает только `practice_game_runs` (и `users`/`teacher_students` для разрезов). Лиг/бейджей/стриков не использует.

**Action в #48:** проверить, что после удаления `leagues/badges/streaks` лидерборд продолжает работать — должен. Никаких правок может не потребоваться.

---

## 4. Биллинг (#49)

### Модели под удаление
| Модель | Файл |
|---|---|
| `Subscription` | `app/Models/Subscription.php` |
| `PayoutItem` | `app/Models/PayoutItem.php` |
| `TeacherPayout` | `app/Models/TeacherPayout.php` |

### Точки правки
| Файл | Что |
|---|---|
| `app/Models/User.php` | `subscriptions()`, `hasActiveSubscription()`, `subscription_plan` accessor (lines 102, 224, 229) |

### БД
Миграция `2026_01_02_000007_create_subscription_tables.php`:
- `subscriptions`, `teacher_payouts`, `payout_items`

Все на проде = **0 строк**, безопасно.

---

## 5. Реферальная программа — TRIP-WIRE (KEEP) (#49)

| Сущность | Статус |
|---|---|
| `app/Models/ReferralClick.php` | KEEP |
| `referral_clicks` таблица | KEEP |
| `app/Http/Controllers/Api/OAuthController.php` | KEEP (строки 7, 66, 88, 185 — отслеживание кликов) |
| Миграция `2026_01_02_000009_create_referral_tables.php` | KEEP |

**В коде используется только в OAuthController.** Никаких выплат, реферальных бонусов — только статистика. Соответствует решению пользователя.

---

## 6. AI-помощник (#50)

**Хороших новостей:** AI-помощник в коде **уже отсутствует**.
- Нет `AiAssistant`, `AssistantService`, классов с `Anthropic`/`OpenAI`/`GPT` SDK
- В views нет упоминаний AI-помощника

**Что осталось:**
- Колонка `users.has_ai_addon` (boolean) в миграции `2026_01_02_000001_update_users_table.php`

**#50 сводится к одной миграции — `dropColumn('has_ai_addon')`.**

---

## 7. Маркетинг (#51)

**В views всё уже чисто:**
- `пазл/puzzle` в blade-шаблонах **не найдено** (грепы дали 0 хитов в `resources/views/`)
- Тарифы 399/799/1199 ₽ — **не найдено** в views
- `Route::*subscription/duel/league/badge/streak/payout*` — нет таких роутов

**Остаются возможные точки:**
- Старая админская панель: `dashboard.blade.php` (badges_count) и `layouts/dashboard.blade.php` (current_streak) — UI элементы, которые надо убрать после #47
- README/docs/маркетинговые материалы (если есть)

#51 → проверить вручную после #47, скорее всего работа минимальна.

---

## 8. Parent App — TRIP-WIRE (KEEP)

| Сущность | Статус |
|---|---|
| `app/Http/Controllers/ParentAppController.php` | KEEP |
| `parent_student` таблица (1 строка на проде) | KEEP |
| `User::parents()`, `User::children()` (lines 178, 186) | KEEP |
| `routes/web.php:446-449` (parent dashboard, child skills/attendance/homework) | KEEP |
| `app/Console/Commands/QaSetupUsersCommand.php` (lines 54-65) | KEEP — QA-сидер |

Parent app использует `student_topic_mastery`, `lesson_attendance`, `homework` — всё это **не пересекается** с выпиливаемыми сущностями.

---

## 9. Telegram Stars — открытый вопрос ⚠️

`StarTransaction` модель формально не геймификация — это **регистрация платежей через Telegram Stars** в боте.

**Использование:**
- `app/Http/Controllers/Auth/TelegramBotAuthController.php` (строки 869, 885, 902): создаёт записи при `pre_checkout_query` и `successful_payment` от Telegram
- Связана с `users.tg_premium_until`, `users.tg_trial_used`, `users.star_balance`
- Команда `app/Console/Commands/PremiumGiftCommand.php` использует `UserGift` (а не StarTransaction напрямую)

**На проде:** `star_transactions` = **0 строк** (фича не запущена).

**Решение нужно от пользователя** (см. ниже).

---

## Открытые вопросы для уточнения

1. **Telegram Stars (`StarTransaction` + `tg_premium_until`/`tg_trial_used`/`star_balance` + premium handler в TelegramBotAuthController) — выпиливаем целиком или оставляем как «спящую» интеграцию?**
   - За выпил: фича не запущена (0 строк), монетизация через TG Stars не входит в новую концепцию
   - За оставить: TG Stars — потенциально удобный канал монетизации в будущем, удалять и потом восстанавливать дороже
   - Рекомендация аудита: **выпилить вместе с #49 (биллинг)** — соответствует «сначала бесплатно, монетизацию позже»

2. **`topics` таблица** (создана в `2026_01_02_000003`, та же миграция что и пазлы) — это какие topics? Если legacy для пазлов — drop. Если используется текущим JSON-стеком — keep. Уточнить в #46 при детальной проработке.

3. **`task_skills` связка** — что произойдёт со скиллами после drop `tasks`? Скиллы остаются, но потеряют связь с задачами. Это OK или нужна миграция reset/rebuild? Скорее всего OK, поскольку привязка `task_skills.task_id → tasks.id` теряет смысл после удаления legacy `tasks`.

---

## Сводная таблица «что куда»

| # | Подзадача | Drop таблиц | Удалить моделей | Править файлов |
|---|---|---|---|---|
| #46 Пазлы | 8 (tasks, task_steps, step_blocks, puzzle_templates, task_skills, attempts, attempt_steps, step_block_selections) | 7 | ~3 (Api/TaskController, GenerateOgeTasks, GeneratePuzzles) |
| #47 Геймификация | 11 (user_streaks, badges, user_badges, leagues, league_participants, duels, duel_tasks, challenges, challenge_teams, challenge_team_members, user_gifts) | 8 | ~7 (User.php, Api/AuthController, Pwa/StudentController, Pwa/VprController, DeleteTelegramUserCommand, PremiumGiftCommand, dashboard views) |
| #48 Стабилизация мини-игр | 0 | 0 | 0 (только проверка) |
| #49 Биллинг | 3 (subscriptions, teacher_payouts, payout_items) + ?TG Stars | 3 | 1 (User.php) |
| #50 AI-помощник | 0 | 0 | 1 (миграция drop колонки `has_ai_addon`) |
| #51 Маркетинг | 0 | 0 | 2-3 (dashboard views) |
| #52 Product KB | 0 | 0 | 3+ (.claude/product/OVERVIEW.md, modules, CLAUDE.md) |

**Итого drop таблиц: ~22.**
**Итого удалить моделей: ~18.**
**Итого правок в коде: ~17 файлов + 7-9 миграций dropColumn.**

---

## Рекомендуемый порядок исполнения

Аудит подтверждает порядок из плана:

1. #46 (пазлы) — самый объёмный, делать первым пока контекст свежий
2. #47 (геймификация) — независим от #46
3. #48 — пятиминутная проверка после #47
4. #49 (биллинг) — независим от #46/#47, но требует уточнения про TG Stars
5. #50 (AI-аддон) — одна миграция, можно склеить с #49
6. #51 (маркетинг) — после #46/#47, минимальный объём
7. #52 (product KB) — финал

#46, #47, #49 можно делать в любом порядке/параллельно — между ними нет зависимостей кроме общей цели.
