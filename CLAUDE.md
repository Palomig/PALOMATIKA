# PALOMATIKA - EdTech Platform for OGE Math Preparation

## О проекте

**PALOMATIKA** — это EdTech SaaS платформа для подготовки к ОГЭ по математике (российский экзамен для 9 класса).

**Ключевая идея:** Уникальная система обучения через "пазлы" — ученики не вводят ответы вручную, а собирают решение из готовых блоков, что снижает порог входа и делает процесс более интерактивным.

**Production URL:** https://cw95865.tmweb.ru (Timeweb hosting)

---

## 🚨 КРИТИЧЕСКИ ВАЖНЫЕ ПРИНЦИПЫ

### 1. SVG — основа проекта

**ВСЕ графические элементы (координатные прямые, геометрические фигуры, графики) должны быть реализованы как SVG.**

- ❌ **НИКОГДА** не использовать PNG/JPEG изображения из PDF
- ✅ **ВСЕГДА** создавать собственные SVG, которые визуально соответствуют оригиналу из PDF
- PDF изображения (`docs/oge_data/images/`) — это **только референс для сверки**, не для использования на сайте

**Почему SVG:**
- Масштабируемость на любых устройствах
- Возможность интерактивности (hover, анимации)
- Меньший размер файлов
- Единый стиль оформления
- Возможность динамической генерации

### 2. Единый источник данных

- Все задания хранятся в JSON: `storage/app/tasks/topic_{id}.json`
- `TaskDataService` — единственный сервис для доступа к данным
- Никаких захардкоженных данных в контроллерах или views

### 3. PDF — только референс

**Директория референсов:** `docs/oge_data/images/`

Изображения из PDF используются ТОЛЬКО для:
- Сверки SVG с оригиналом
- Проверки правильности отображения
- Понимания структуры задания

**Формат имён:** `oge{номер_темы}_p{страница}_img{номер}.png`

---

## Технический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| PHP | 8.2+ | Backend |
| Laravel | 10 LTS | Framework |
| MySQL | 8.0 | База данных |
| Laravel Sanctum | - | API аутентификация |
| Laravel Socialite | - | OAuth (VK, Telegram) |
| Tailwind CSS | 3.x (CDN) | Стили |
| Alpine.js | 3.x (CDN) | Интерактивность |

**Важно:** Без сборщиков (Vite/Webpack). Всё через CDN для простоты деплоя.

---

## Структура проекта

### Модели (28 штук)
```
app/Models/
├── User.php              # Пользователи (ученики, учителя, админы)
├── Topic.php             # Темы ОГЭ (геометрия, алгебра и т.д.)
├── Skill.php             # Навыки внутри тем
├── Task.php              # Задания
├── TaskStep.php          # Шаги задания (пазл-этапы)
├── StepBlock.php         # Блоки для сборки (варианты ответов)
├── PuzzleTemplate.php    # Шаблоны пазлов
├── Attempt.php           # Попытки решения
├── AttemptStep.php       # Шаги попытки
├── StepBlockSelection.php # Выбранные блоки
├── UserSkill.php         # Прогресс по навыкам
├── UserStreak.php        # Серии (streak)
├── UserDailyStat.php     # Ежедневная статистика
├── Badge.php             # Достижения
├── UserBadge.php         # Полученные достижения
├── League.php            # Лиги (Bronze, Silver, Gold и т.д.)
├── LeagueParticipant.php # Участники лиг
├── Duel.php              # Дуэли между учениками
├── Challenge.php         # Командные челленджи
├── ChallengeTeam.php     # Команды в челленджах
├── Homework.php          # Домашние задания
├── HomeworkAssignment.php # Назначения ДЗ
├── TeacherStudent.php    # Связь учитель-ученик
├── Subscription.php      # Подписки
├── TeacherPayout.php     # Выплаты учителям
├── PayoutItem.php        # Позиции выплат
├── ReferralClick.php     # Клики по реферальным ссылкам
└── ActivityLog.php       # Логи активности
```

### Контроллеры API (10 штук)
```
app/Http/Controllers/Api/
├── AuthController.php       # Регистрация, вход, профиль
├── OAuthController.php      # OAuth для API
├── TopicController.php      # Темы и прогресс
├── SkillController.php      # Навыки
├── TaskController.php       # Задания и попытки
├── ProgressController.php   # Дашборд, история
├── BadgeController.php      # Достижения
├── LeaderboardController.php # Рейтинги, лиги
├── HomeworkController.php   # Домашние задания
└── DuelController.php       # Дуэли
```

### Контроллеры Web
```
app/Http/Controllers/Auth/
└── SocialAuthController.php # OAuth (VK, Telegram) для web
```

### Views (17 штук)
```
resources/views/
├── welcome.blade.php           # Landing page
├── dashboard.blade.php         # Дашборд ученика
├── layouts/
│   ├── auth.blade.php          # Layout для авторизации
│   ├── app.blade.php           # Layout для ученика
│   └── teacher.blade.php       # Layout для учителя
├── auth/
│   ├── login.blade.php         # Страница входа
│   └── register.blade.php      # Страница регистрации
├── student/
│   ├── topics.blade.php        # Список тем
│   ├── practice.blade.php      # Решение задач (пазлы)
│   ├── leaderboard.blade.php   # Рейтинг
│   ├── badges.blade.php        # Достижения
│   └── duels.blade.php         # Дуэли
└── teacher/
    ├── dashboard.blade.php     # Дашборд учителя
    ├── students.blade.php      # Список учеников
    ├── homework.blade.php      # Домашние задания
    ├── analytics.blade.php     # Аналитика
    └── earnings.blade.php      # Заработок
```

### Миграции (14 штук)
```
database/migrations/
├── 2014_10_12_000000_create_users_table.php
├── 2026_01_02_000001_update_users_table.php      # OAuth, роли, подписки
├── 2026_01_02_000002_create_skills_tables.php    # topics, skills
├── 2026_01_02_000003_create_tasks_tables.php     # tasks, steps, blocks
├── 2026_01_02_000004_create_attempts_tables.php  # attempts
├── 2026_01_02_000005_create_gamification_tables.php # badges, leagues, duels
├── 2026_01_02_000006_create_homework_tables.php
├── 2026_01_02_000007_create_subscription_tables.php
├── 2026_01_02_000008_create_activity_tables.php
├── 2026_01_02_000009_create_referral_tables.php
└── 2026_01_03_000001_make_email_password_nullable.php # Для OAuth
```

### Seeders (5 штук)
```
database/seeders/
├── DatabaseSeeder.php   # Главный сидер
├── LeagueSeeder.php     # 6 лиг (Bronze → Champion)
├── TopicSeeder.php      # Темы ОГЭ
├── SkillSeeder.php      # Навыки по темам
├── BadgeSeeder.php      # 15+ достижений
└── TaskSeeder.php       # Примеры заданий
```

---

## API Endpoints

### Публичные
```
POST /api/auth/login          # Вход
POST /api/auth/register       # Регистрация
GET  /api/topics              # Список тем
GET  /api/skills              # Список навыков
GET  /api/badges              # Список достижений
GET  /api/leaderboard/leagues # Лиги
```

### Защищённые (требуют токен)
```
GET  /api/auth/me                    # Текущий пользователь
GET  /api/tasks/next                 # Следующее задание
POST /api/tasks/{id}/start           # Начать попытку
POST /api/attempts/{id}/submit       # Отправить ответ
GET  /api/progress/dashboard         # Статистика
GET  /api/badges/user                # Мои достижения
GET  /api/homework                   # Мои ДЗ
POST /api/duels                      # Создать дуэль
```

---

## Роли пользователей

| Роль | Описание |
|------|----------|
| `student` | Ученик - решает задачи, участвует в дуэлях |
| `teacher` | Учитель - создаёт ДЗ, отслеживает учеников, получает % |
| `admin` | Администратор - полный доступ |

---

## Система подписок

| План | Цена | Возможности |
|------|------|-------------|
| `free` | 0₽ | 5 задач/день, базовая статистика |
| `start` | 290₽/мес | Безлимит задач, все темы |
| `standard` | 490₽/мес | + ДЗ от учителя, детальная аналитика |
| `premium` | 790₽/мес | + ИИ-подсказки, приоритетная поддержка |

**Trial:** 7 дней бесплатно при регистрации

---

## Гейм ификация

### Лиги (еженедельный рейтинг)
1. Bronze (0-99 XP)
2. Silver (100-299 XP)
3. Gold (300-599 XP)
4. Platinum (600-999 XP)
5. Diamond (1000-1499 XP)
6. Champion (1500+ XP)

### Достижения (badges)
- Первые шаги (решить первую задачу)
- Серия 7 дней
- Серия 30 дней
- 100 задач решено
- Мастер темы
- Победитель дуэли
- И другие...

### Дуэли
Соревнование 1 на 1: кто быстрее и правильнее решит 5 задач.

---

## OAuth Провайдеры

### Telegram (работает)
```env
TELEGRAM_BOT_TOKEN=xxx
TELEGRAM_BOT_USERNAME=palomatika_auth_bot
```
- Использует Telegram Login Widget
- Домен настроен в @BotFather

### VKontakte (требуется настройка)
```env
VK_CLIENT_ID=xxx
VK_CLIENT_SECRET=xxx
VK_REDIRECT_URI=https://cw95865.tmweb.ru/auth/vkontakte/callback
```

---

## Деплой

**Хостинг:** Timeweb (cw95865.tmweb.ru)
**Путь на сервере:** /home/c/cw95865/OGE/

### Автоматический CI/CD

На GitHub настроен автоматический workflow:
1. **Auto-merge:** Ветки `claude/*` автоматически мержатся в `main` после push
2. **Auto-deploy:** После merge в `main` автоматически выполняется deploy на production сервер

**ВАЖНО:** Любые изменения в ветках `claude/*` попадут на production автоматически! Тестируйте локально перед push.

### Команды для ручного деплоя
```bash
# На сервере
cd /home/c/cw95865/OGE
git pull origin main
php artisan migrate --force
php artisan config:clear
php artisan cache:clear
php artisan route:clear
```

---

## ЧТО СДЕЛАНО ✅

### Backend
- [x] 28 Eloquent моделей с relationships
- [x] 14 миграций
- [x] 10 API контроллеров
- [x] 5 seeders с тестовыми данными
- [x] Laravel Sanctum аутентификация
- [x] OAuth через Telegram (работает)
- [x] Реферальная система

### Frontend
- [x] Landing page (welcome.blade.php)
- [x] Страницы авторизации (login, register)
- [x] Layout для учеников (app.blade.php)
- [x] Layout для учителей (teacher.blade.php)
- [x] Дашборд ученика
- [x] 5 страниц ученика (topics, practice, leaderboard, badges, duels)
- [x] 5 страниц учителя (dashboard, students, homework, analytics, earnings)

### Интеграции
- [x] Telegram Login Widget
- [x] Роуты OAuth

---

## ЧТО ПРЕДСТОИТ СДЕЛАТЬ 📋

### Приоритет 1: Редизайн (Dark Theme)

**Целевой дизайн:** Тёмная тема как в приложении Pomo (см. референс)

#### Цветовая палитра
```css
/* Фон */
--bg-primary: #1a1a2e;      /* Основной фон */
--bg-secondary: #252542;     /* Фон карточек */
--bg-tertiary: #2d2d4a;      /* Hover состояния */

/* Акценты */
--accent-primary: #ff6b6b;   /* Коралловый - кнопки, важные элементы */
--accent-secondary: #4ade80; /* Зелёный - успех, прогресс */
--accent-tertiary: #60a5fa;  /* Синий - ссылки, инфо */

/* Текст */
--text-primary: #ffffff;     /* Основной текст */
--text-secondary: #9ca3af;   /* Вторичный текст */
--text-muted: #6b7280;       /* Приглушённый */

/* Границы */
--border-color: #374151;     /* Границы карточек */
```

#### Элементы дизайна
- Закруглённые карточки (rounded-2xl, rounded-3xl)
- Тени с эффектом свечения
- Иконки в кругах с акцентным фоном
- Плавные переходы и анимации
- Минималистичный современный стиль

#### Страницы для обновления
1. [ ] layouts/auth.blade.php - тёмный фон, coral акценты
2. [ ] auth/login.blade.php - тёмные поля ввода
3. [ ] auth/register.blade.php - тёмные поля ввода
4. [ ] layouts/app.blade.php - тёмный sidebar, карточки
5. [ ] dashboard.blade.php - тёмные виджеты
6. [ ] student/*.blade.php - все страницы ученика
7. [ ] layouts/teacher.blade.php - тёмная тема
8. [ ] teacher/*.blade.php - все страницы учителя
9. [ ] welcome.blade.php - тёмный landing

### Приоритет 2: Функциональность

- [ ] Интеграция оплаты (ЮKassa или Robokassa)
- [ ] Email-уведомления
- [ ] Добавить больше заданий в базу
- [ ] Push-уведомления
- [ ] PWA (Progressive Web App)

### Приоритет 3: Улучшения

- [ ] Настройка VK OAuth
- [ ] Админ-панель для управления контентом
- [ ] Экспорт статистики
- [ ] Мобильная адаптация (улучшить)

---

## Команды для разработки

```bash
# Локальная разработка
php artisan serve

# Миграции
php artisan migrate:fresh --seed

# Очистка кэша
php artisan config:clear && php artisan cache:clear && php artisan route:clear

# Проверка роутов
php artisan route:list

# Тестирование API
curl http://localhost:8000/api/topics
curl http://localhost:8000/api/badges
```

---

## Важные файлы конфигурации

### .env (основные переменные)
```env
APP_NAME=PALOMATIKA
APP_URL=https://cw95865.tmweb.ru
APP_ENV=production

DB_CONNECTION=mysql
DB_DATABASE=palomatika

TELEGRAM_BOT_TOKEN=xxx
TELEGRAM_BOT_USERNAME=palomatika_auth_bot

VK_CLIENT_ID=xxx
VK_CLIENT_SECRET=xxx
```

### config/services.php
```php
'telegram' => [
    'bot_username' => env('TELEGRAM_BOT_USERNAME'),
    'bot_token' => env('TELEGRAM_BOT_TOKEN'),
],
'vkontakte' => [
    'client_id' => env('VK_CLIENT_ID'),
    'client_secret' => env('VK_CLIENT_SECRET'),
    'redirect' => env('VK_REDIRECT_URI'),
],
```

---

## Контакты и ссылки

- **Production:** https://cw95865.tmweb.ru
- **Репозиторий:** GitHub (Palomig/PALOMATIKA)
- **Хостинг:** Timeweb

---

## 📁 PDF-источники заданий ОГЭ

**Директория:** `pdf-sources/oge-trainer/`

Содержит оригинальные PDF файлы с заданиями ОГЭ для создания базы заданий.

### Список файлов

| Файл | Описание |
|------|----------|
| `ОГЭ 2026 №01-05 Квартира (трен).pdf` | Комплексное задание: Квартира |
| `ОГЭ 2026 №01-05 Листы бумаги (трен).pdf` | Комплексное задание: Листы бумаги |
| `ОГЭ 2026 №01-05 Печь для бани (трен).pdf` | Комплексное задание: Печь для бани |
| `ОГЭ 2026 №01-05 План местности (трен).pdf` | Комплексное задание: План местности |
| `ОГЭ 2026 №01-05 Тарифы (трен).pdf` | Комплексное задание: Тарифы |
| `ОГЭ 2026 №01-05 Участок (трен).pdf` | Комплексное задание: Участок |
| `ОГЭ 2026 №01-05 Шины (трен по 5).pdf` | Комплексное задание: Шины |
| `ОГЭ 2026 Задание №06 (трен) v2.pdf` | Дроби и степени |
| `ОГЭ 2026 Задание №07 (трен).pdf` | Числа, координатная прямая |
| `ОГЭ 2026 Задание №08 (трен).pdf` | Квадратные корни и степени |
| `ОГЭ 2026 Задание №09 (трен).pdf` | Уравнения |
| `ОГЭ 2026 Задание №10 (трен).pdf` | Теория вероятностей |
| `ОГЭ 2026 Задание №11 (трен).pdf` | Графики функций |
| `ОГЭ 2026 Задание №12 (трен).pdf` | Расчёты по формулам |
| `ОГЭ 2026 Задание №13 (трен).pdf` | Неравенства |
| `ОГЭ 2026 Задание №14 (трен).pdf` | Прогрессии |
| `ОГЭ 2026 Задание №15 (трен).pdf` | Треугольники |
| `ОГЭ 2026 Задание №16 (трен).pdf` | Окружность |
| `ОГЭ 2026 Задание №17 (трен) v2.pdf` | Четырёхугольники |
| `ОГЭ 2026 Задание №18 (трен).pdf` | Фигуры на клетчатой бумаге |
| `ОГЭ 2026 Задание №19 (трен).pdf` | Анализ геом. высказываний |
| `ОГЭ 2026 Задание №20 (трен) v2.pdf` | Задание 20 |
| `ОГЭ 2026 Задание №21 (трен).pdf` | Задание 21 |
| `ОГЭ 2026 Задание №22 (трен).pdf` | Задание 22 |
| `ОГЭ 2026 Задание №23 (трен).pdf` | Задание 23 |
| `ОГЭ 2026 Задание №24 (трен).pdf` | Задание 24 |
| `ОГЭ 2026 Задание №25 (трен).pdf` | Задание 25 |

**Примечание:** Файлы с суффиксом `(трен по 5)` содержат расширенные варианты заданий.

**ВАЖНО:** Директория `pdf-sources/` указана в `.gitignore` и не коммитится. Референсные изображения находятся в `docs/oge_data/images/` (см. раздел "КРИТИЧЕСКИ ВАЖНЫЕ ПРИНЦИПЫ" в начале документа).

---

## История изменений

### 2026-01-11
- **Архитектура:** Создан `TaskDataService` — единый источник данных для заданий
- **JSON-хранилище:** Задания теперь хранятся в `storage/app/tasks/topic_{id}.json`
- **Унифицированный layout:** Создан `layouts/topic.blade.php` для всех страниц тем
- **Инструмент проверки:** Добавлен `task-review-tool` для пометки проблемных заданий
- **KaTeX:** Добавлен `\displaystyle` ко всем дробям (включая вложенные) для крупного отображения

### 2026-01-04
- Настроен Telegram Login Widget
- Исправлен порядок OAuth роутов
- Email и password сделаны nullable для OAuth пользователей

### 2026-01-03
- Созданы все seeders
- Добавлены страницы учителя
- Добавлены страницы ученика
- Создан landing page

### 2026-01-02
- Инициализация проекта
- Созданы все миграции и модели
- Созданы API контроллеры

---

## TODO: Будущие задачи

### 🔴 ВАЖНО: Редактор заданий (отложено)

**Описание:** Создать веб-интерфейс для полуавтоматического редактирования распарсенных PDF.

**Функционал:**
- Парсер извлекает базовую структуру из PDF (блоки, задания, изображения)
- Веб-редактор позволяет корректировать данные:
  - Редактирование инструкций заданий
  - LaTeX-редактор с live preview для математических выражений
  - Drag-and-drop маппинг изображений к задачам
  - Добавление/удаление задач и блоков
  - Выбор типа задания (choice, expression, interval и т.д.)
- Валидация данных перед сохранением
- Экспорт в JSON / сохранение в БД

**Причина отложения:** Сначала нужно создать базу заданий вручную для всех тем.

**Важно:** Автоматический парсер (`AdvancedPdfParser`) не справляется с полным парсингом PDF-файлов из-за сложной структуры:
- Дроби в PDF отображаются как числитель над знаменателем (разные строки)
- Математические выражения требуют ручной конвертации в LaTeX
- Изображения нужно вручную связывать с задачами
- Разный формат данных в разных PDF файлах

**Решение:** Требуется вмешательство человека или ИИ для ручного создания структурированных данных на основе PDF.

**Файлы для справки:**
- `app/Services/AdvancedPdfParser.php` - текущий автопарсер
- `app/Http/Controllers/TestPdfController.php` - контроллер с примером данных
- `/test/6`, `/test/7` - примеры готовых страниц

---

### 📚 База заданий ОГЭ

**Статус парсинга PDF файлов:**

| Задание | Статус | Страница | Примечание |
|---------|--------|----------|------------|
| 01-05 Квартира | ⏳ | - | Комплексное задание |
| 01-05 Листы бумаги | ⏳ | - | Комплексное задание |
| 01-05 Печь для бани | ⏳ | - | Комплексное задание |
| 01-05 План местности | ⏳ | - | Комплексное задание |
| 01-05 Тарифы | ⏳ | - | Комплексное задание |
| 01-05 Участок | ⏳ | - | Комплексное задание |
| 01-05 Шины | ⏳ | - | Комплексное задание |
| 06 Дроби и степени | ✅ | /test/6 | Готово |
| 07 Числа, координатная прямая | ✅ | /test/7 | Готово |
| 08 Квадратные корни и степени | ✅ | /test/8 | Готово |
| 09 Уравнения | ✅ | /test/9 | Готово |
| 10 Теория вероятностей | ✅ | /test/10 | Готово |
| 11 Графики функций | ✅ | /test/11 | Готово (SVG графики) |
| 12 Расчёты по формулам | ✅ | /test/12 | Готово |
| 13 Неравенства | ✅ | /test/13 | Готово |
| 14 Прогрессии | ✅ | /test/14 | Готово |
| 15 Треугольники | ✅ | /test/15 | Готово |
| 16 Окружность | ✅ | /test/16 | Готово |
| 17 Четырёхугольники | ✅ | /test/17 | Готово |
| 18 Фигуры на клетчатой бумаге | ✅ | /test/18 | Готово |
| 19 Анализ геом. высказываний | ✅ | /test/19 | Готово |
| 20 | ⏳ | - | - |
| 21 | ⏳ | - | - |
| 22 | ⏳ | - | - |
| 23 | ⏳ | - | - |
| 24 | ⏳ | - | - |
| 25 | ⏳ | - | - |

**Легенда:** ✅ Готово | 🔄 В работе | ⏳ Ожидает

**PDF файлы находятся в git истории:** `git show b8c1426:"РиР_ ОГЭ (тренажер)/..."`

---

## 🗂️ База заданий ОГЭ — Архитектура

### Единый источник данных (Single Source of Truth)

**Архитектура:** Все задания хранятся в JSON-файлах и управляются через `TaskDataService`.

```
storage/app/tasks/
├── topic_06.json    # Дроби и степени
├── topic_07.json    # Числа, координатная прямая
├── topic_08.json    # Квадратные корни
├── ...
└── topic_19.json    # Геом. высказывания
```

### TaskDataService — Центральный сервис

**Файл:** `app/Services/TaskDataService.php`

**Назначение:** Единая точка доступа к данным заданий для всех модулей:
- Web-страницы тем (`/test/6`, `/test/7`, ...)
- Генератор ОГЭ (`/oge/{hash}`)
- API для мобильного приложения
- Инструмент проверки заданий

**Основные методы:**

```php
$service = app(TaskDataService::class);

// Получить данные темы
$blocks = $service->getBlocks('06');

// Получить метаданные темы
$meta = $service->getTopicMeta('06');
// → ['title' => 'Дроби и степени', 'color' => 'blue', ...]

// Получить статистику
$stats = $service->getTopicStats('06');
// → ['blocks' => 5, 'zadaniya' => 20, 'tasks' => 174]

// Получить случайные задания (для генератора ОГЭ)
$tasks = $service->getRandomTasks('06', count: 3);

// Сохранить данные (для редактора)
$service->saveTopicData('06', $data);

// Очистить кэш
$service->clearCache();
```

### Структура JSON-файла темы

```json
{
  "topic_id": "06",
  "blocks": [
    {
      "number": 1,
      "title": "ФИПИ",
      "zadaniya": [
        {
          "number": 1,
          "instruction": "Найдите значение выражения",
          "type": "expression",
          "tasks": [
            {
              "id": 1,
              "expression": "\\frac{3}{4} \\cdot \\frac{6}{5}",
              "answer": "0.9"
            }
          ]
        }
      ]
    }
  ]
}
```

### Типы заданий (type)

| Тип | Описание | Темы |
|-----|----------|------|
| `expression` | Вычисление выражения | 06, 08 |
| `choice` | Выбор из вариантов | 07 |
| `simple_choice` | Простой выбор | 07 |
| `fraction_choice` | Выбор дроби | 07 |
| `interval_choice` | Выбор интервала | 07 |
| `matching` | Соответствие графиков | 11 |
| `matching_signs` | Соответствие знаков | 11 |
| `word_problem` | Текстовая задача | 10, 14 |
| `geometry` | Геометрия | 15, 16, 17 |
| `grid` | Клетчатая бумага | 18 |
| `statements` | Анализ утверждений | 19 |

### Унифицированный layout для тем

**Файл:** `resources/views/layouts/topic.blade.php`

Единый шаблон для всех страниц тем с:
- Навигацией между темами
- Статистикой (блоки, задания)
- Инструментом проверки заданий
- KaTeX с поддержкой `\displaystyle` для дробей

**Использование в контроллере:**

```php
public function showTopic(string $id)
{
    $service = app(TaskDataService::class);
    $topicId = str_pad($id, 2, '0', STR_PAD_LEFT);

    return view('topics.show', [
        'topicId' => $topicId,
        'topicMeta' => $service->getTopicMeta($topicId),
        'blocks' => $service->getBlocks($topicId),
        'stats' => $service->getTopicStats($topicId),
    ]);
}
```

### Миграция данных (статус)

**Текущее состояние:** Данные частично в PHP-массивах (`TestPdfController.php`), частично в JSON.

**Целевое состояние:** Все данные в JSON-файлах, PHP-массивы удалены.

| Тема | JSON | PHP-массив | Примечание |
|------|------|------------|------------|
| 06-19 | ⏳ | ✅ | Требуется миграция |

**Команда для миграции одной темы:**
```bash
php artisan task:migrate-to-json 06
```

### Views (страницы отображения)

```
resources/views/
├── topics/
│   └── show.blade.php      # Унифицированная страница темы
├── layouts/
│   └── topic.blade.php     # Layout для страниц тем
├── components/
│   └── task-review-tool.blade.php  # Инструмент проверки
└── test/
    ├── index.blade.php          # Список всех тем /test
    ├── topic06.blade.php        # Legacy: /test/6 (использует PHP-массивы)
    ├── ...
    ├── oge-generator.blade.php  # Генератор ОГЭ /oge
    └── oge-variant.blade.php    # Вариант ОГЭ /oge/{hash}
```

### Роуты

```php
// Новые унифицированные роуты
Route::get('/topics', [TopicController::class, 'index'])->name('topics.index');
Route::get('/topics/{id}', [TopicController::class, 'show'])->name('topics.show');

// Legacy роуты (для обратной совместимости)
Route::get('/test', [TestPdfController::class, 'index'])->name('test.index');
Route::get('/test/{id}', [TestPdfController::class, 'topic'])->name('test.topic');

// Генератор вариантов ОГЭ
Route::get('/oge', [TestPdfController::class, 'ogeGenerator'])->name('test.oge.generator');
Route::get('/oge/{hash}', [TestPdfController::class, 'showOgeVariant'])->name('test.oge.show');
```

### Генератор вариантов ОГЭ

**URL:** `/oge` — страница генератора
**URL:** `/oge/{hash}` — сгенерированный вариант (10-символьный хэш)

**Как работает:**
1. Использует `TaskDataService::getRandomTasks()` для выбора заданий
2. Хэш используется как seed для `mt_srand()` — детерминированная генерация
3. Один и тот же хэш всегда даёт одинаковый вариант
4. Можно делиться ссылками на варианты

### Изображения для заданий

```
public/images/tasks/
├── 11/           # Графики для темы 11
├── 15/           # Геометрия для темы 15
├── 16/           # Геометрия для темы 16
├── 17/           # Геометрия для темы 17
└── 18/           # Клетчатая бумага для темы 18
```

### KaTeX: Отображение формул

Все страницы с математическими формулами используют:

```javascript
// Автоматическое добавление \displaystyle ко всем дробям
function renderMathWithDisplayStyle() {
    document.querySelectorAll('body *').forEach(el => {
        if (el.childNodes.length === 1 && el.childNodes[0].nodeType === 3) {
            let text = el.childNodes[0].textContent;
            if (text.includes('$') && text.includes('\\frac')) {
                text = text.replace(/\\frac\{/g, '\\displaystyle\\frac{');
                el.childNodes[0].textContent = text;
            }
        }
    });
    renderMathInElement(document.body, {...});
}
```

Это обеспечивает крупное отображение всех дробей, включая вложенные (трёхэтажные).

### Как добавить новые задания

1. Создать/отредактировать JSON файл `storage/app/tasks/topic_{id}.json`
2. Следовать структуре данных (см. выше)
3. Очистить кэш: `php artisan cache:clear`
4. Для изображений — добавить файлы в `public/images/tasks/{topic_id}/`

---

## 📐 GEOMETRY_SPEC — Правила создания SVG для геометрии

### Базовые принципы

1. **Все координаты хранятся как объекты** `{x, y}`
2. **Центр фигуры** — центроид (для треугольника: среднее арифметическое координат вершин)
3. **ViewBox** — рекомендуется `0 0 300 220` для треугольников
4. **Отступы от краёв** — минимум 30px, чтобы подписи не обрезались
5. **Концептуальные диаграммы** — рисунок показывает СУТЬ задачи, а не точные значения

### Стандартные размеры SVG диаграмм

При создании или изменении SVG диаграмм для заданий:

| Параметр | Стандартное значение |
|----------|---------------------|
| `max-w-[...]` | **250px** (всегда фиксированный!) |
| `viewBox` | Пропорционален содержимому (200-275 по ширине) |

**Правило масштабирования:**
1. `max-w-[250px]` — **ФИКСИРОВАННЫЙ** размер для ВСЕХ диаграмм, НЕ менять!
2. `viewBox` — масштабируется под содержимое
3. При запросе "увеличить масштаб" — увеличивать координаты внутри `viewBox`, НЕ менять `max-w`

**Пример правильного SVG:**
```html
<svg viewBox="0 0 250 200" class="w-full max-w-[250px] h-auto">
    <!-- содержимое -->
</svg>
```

### Правило заполнения viewBox (85%)

**ВАЖНО:** Геометрическая фигура должна заполнять **~85%** площади viewBox, чтобы рисунок был крупным и читаемым.

| viewBox | Стандартный размер | Заполнение фигуры |
|---------|-------------------|-------------------|
| `0 0 220 200` | Универсальный | Ширина ~187px, высота ~170px |

**Расчёт для вписанной окружности (трапеция):**
- `r = 70` → высота фигуры = 140px (70% от 200)
- Ширина трапеции: от x=1 до x=219 (99% от 220)
- Формула касания: `a × b = r²`, где a, b — полуширины оснований

**Расчёт для квадрата:**
- Сторона квадрата = 150px → 75% высоты viewBox
- Радиус вписанной окружности = 75px

**Расчёт для треугольника:**
- Высота треугольника ~140-160px (70-80% от 200)
- Ширина основания ~180-200px (82-91% от 220)

**Минимальные отступы от края viewBox:**
- Для подписей вершин (A, B, C): минимум **25px** от края
- Для фигуры без подписей: минимум **5px** от края

**Чек-лист при создании SVG:**
- [ ] Фигура занимает ~85% площади viewBox
- [ ] Подписи вершин не обрезаются (отступ 25px)
- [ ] Окружности полностью помещаются в viewBox
- [ ] viewBox = `0 0 220 200` (стандарт)
- [ ] max-w = `250px` (фиксированный)

### Правило концептуальных диаграмм

**ВАЖНО:** Геометрический рисунок должен иллюстрировать концепцию задачи, а НЕ буквально отображать числовые значения из условия.

**Что показывает рисунок:**
- Тип фигуры (треугольник, четырёхугольник и т.д.)
- Какие элементы даны (угол, сторона, медиана, биссектриса и т.д.)
- Что требуется найти (отмечается знаком `?` или зелёным цветом)
- Взаимное расположение элементов

**Чего НЕ должен показывать рисунок:**
- Точные пропорции углов и сторон
- Буквальное отображение экстремальных значений (угол 177° → почти плоский треугольник)

**Примеры (углы и стороны):**

| Условие | Неправильно | Правильно |
|---------|-------------|-----------|
| Угол C = 177° | Рисовать почти плоский треугольник | Нормальный треугольник с отмеченным углом C |
| Угол = 3° | Рисовать едва видимый острый угол | Обычный острый угол с подписью |
| Угол BAC = 24° | Рисовать узкий угол, где метка не помещается | Угол ~60° с подписью "24°" |
| Сторона = 1000 | Пытаться показать масштаб | Обычная сторона с подписью длины |
| Равнобедренный с основанием 0.1 | Очень узкий треугольник | Нормальный равнобедренный треугольник |

**Примеры (окружности):**

| Условие | Неправильно | Правильно |
|---------|-------------|-----------|
| Радиус = 2√5, окружность через точку A | Вычислять точный R по формуле расстояния (R = √12500 ≈ 112), что выводит окружность за viewBox | Подобрать R визуально (≈ 60-70), чтобы окружность выглядела проходящей через A и помещалась в viewBox |
| Окружность описана около треугольника | Вычислять точный радиус описанной окружности по формуле R = abc/4S | Нарисовать окружность, визуально проходящую через все три вершины, с разумным радиусом |
| Окружность вписана в треугольник | Вычислять точный радиус r = S/p | Нарисовать окружность, визually касающуюся всех трёх сторон |
| Касательная длиной 15 к окружности радиуса 8 | Пытаться соблюсти пропорцию 15:8 | Показать касательную разумной длины с подписью "15" |

**Принцип:** Рисунок — это схема для понимания задачи, а не чертёж в масштабе. Все конкретные значения указываются текстом (подписями), а форма фигуры должна быть читаемой и понятной.

**Главное правило для окружностей:** Радиус и положение центра подбираются так, чтобы вся фигура помещалась в viewBox с отступами минимум 5px от краёв. Визуальная "правильность" (окружность выглядит проходящей через нужные точки) важнее математической точности.

### Цветовая схема

| Элемент | Цвет | HEX |
|---------|------|-----|
| Основные линии (стороны) | Красный | `#dc2626` |
| Highlight / известные значения | Янтарный | `#f59e0b` |
| Вспомогательные линии (медианы, биссектрисы, высоты) | Зелёный | `#10b981` |
| Вспомогательные элементы (маркеры равенства) | Синий | `#3b82f6` |
| Подписи точек | Голубой | `#60a5fa` |
| Второстепенный текст (длины сторон) | Серый | `#94a3b8` |
| Прямой угол | Серый | `#666666` |

### Функция 1: labelPos() — позиционирование подписей

```javascript
function labelPos(point, center, distance = 22) {
    const dx = point.x - center.x;
    const dy = point.y - center.y;
    const len = Math.sqrt(dx * dx + dy * dy);
    if (len === 0) return { x: point.x, y: point.y - distance };
    return {
        x: point.x + (dx / len) * distance,
        y: point.y + (dy / len) * distance
    };
}
```

**Правила:**
- Подпись располагается **в направлении от центра фигуры**
- Расстояние по умолчанию: **22px**
- Всегда использовать `text-anchor="middle"` и `dominant-baseline="middle"`

### Функция 2: makeAngleArc() — дуга угла

```javascript
function makeAngleArc(vertex, point1, point2, radius) {
    const angle1 = Math.atan2(point1.y - vertex.y, point1.x - vertex.x);
    const angle2 = Math.atan2(point2.y - vertex.y, point2.x - vertex.x);
    const x1 = vertex.x + radius * Math.cos(angle1);
    const y1 = vertex.y + radius * Math.sin(angle1);
    const x2 = vertex.x + radius * Math.cos(angle2);
    const y2 = vertex.y + radius * Math.sin(angle2);
    let angleDiff = angle2 - angle1;
    while (angleDiff > Math.PI) angleDiff -= 2 * Math.PI;
    while (angleDiff < -Math.PI) angleDiff += 2 * Math.PI;
    const sweep = angleDiff > 0 ? 1 : 0;
    return `M ${x1} ${y1} A ${radius} ${radius} 0 0 ${sweep} ${x2} ${y2}`;
}
```

**Правила:**
- `vertex` — вершина угла
- `point1`, `point2` — точки на **сторонах** угла (не произвольные точки!)
- Радиус дуги: **20-35px** в зависимости от размера угла
- Дуга **ВСЕГДА** начинается и заканчивается на сторонах угла

### Функция 3: rightAnglePath() — прямой угол (квадратик)

```javascript
function rightAnglePath(vertex, p1, p2, size = 12) {
    const angle1 = Math.atan2(p1.y - vertex.y, p1.x - vertex.x);
    const angle2 = Math.atan2(p2.y - vertex.y, p2.x - vertex.x);
    const c1 = { x: vertex.x + size * Math.cos(angle1), y: vertex.y + size * Math.sin(angle1) };
    const c2 = { x: vertex.x + size * Math.cos(angle2), y: vertex.y + size * Math.sin(angle2) };
    const diag = { x: c1.x + size * Math.cos(angle2), y: c1.y + size * Math.sin(angle2) };
    return `M ${c1.x} ${c1.y} L ${diag.x} ${diag.y} L ${c2.x} ${c2.y}`;
}
```

**Правила:**
- `vertex` — вершина прямого угла
- `p1`, `p2` — точки на сторонах угла
- Размер квадратика: **12-15px**
- Цвет: серый `#666666`

**ВАЖНО: Порядок p1 и p2 влияет на направление квадратика!**

Квадратик рисуется от p1 к p2 против часовой стрелки. Чтобы квадратик был **внутри** фигуры:
- Точки p1 и p2 должны идти **по часовой стрелке** относительно vertex
- Если квадратик выходит наружу — **поменяйте p1 и p2 местами**

**Пример для прямоугольного треугольника:**
```
      B
     /|
    / |  ← прямой угол в C
   /  |
  A---C
```
- Вершины по часовой: A → B → C → A
- Для угла в C: стороны идут к A и к B
- По часовой от C: сначала B, потом A
- Вызов: `rightAnglePath(C, B, A, 15)` — квадратик внутри
- Вызов: `rightAnglePath(C, A, B, 15)` — квадратик СНАРУЖИ (неправильно!)

**Проверка:** визуально квадратик должен находиться внутри угла треугольника.

### Функция 3.1: isRightAngle() — определение прямого угла

```javascript
function isRightAngle(vertex, p1, p2) {
    const v1 = { x: p1.x - vertex.x, y: p1.y - vertex.y };
    const v2 = { x: p2.x - vertex.x, y: p2.y - vertex.y };
    const dot = v1.x * v2.x + v1.y * v2.y;
    return Math.abs(dot) < 1; // допуск для погрешности округления
}
```

**Назначение:** Определяет, является ли угол в вершине прямым (90°).

**Принцип работы:**
- Вычисляет скалярное произведение векторов от vertex к p1 и от vertex к p2
- Если скалярное произведение ≈ 0, угол = 90° (векторы перпендикулярны)

**Использование перед rightAnglePath():**
```javascript
// Проверяем каждую вершину
if (isRightAngle(A, B, C)) {
    // Прямой угол в A, рисуем квадратик
    rightAnglePath(A, B, C, 15);
} else if (isRightAngle(B, A, C)) {
    rightAnglePath(B, A, C, 15);
} else if (isRightAngle(C, A, B)) {
    rightAnglePath(C, A, B, 15);
}
```

**ВАЖНО:** Всегда используйте `isRightAngle()` для определения вершины прямого угла, а не визуальную оценку координат. Это предотвращает ошибки при неочевидной ориентации треугольника.

### Функция 3.2: equalityTick() — маркер равенства сторон

```javascript
function equalityTick(p1, p2, t = 0.5, length = 8) {
    // Точка на середине отрезка
    const mid = {
        x: p1.x + (p2.x - p1.x) * t,
        y: p1.y + (p2.y - p1.y) * t
    };
    // Вектор вдоль отрезка
    const dx = p2.x - p1.x;
    const dy = p2.y - p1.y;
    const len = Math.sqrt(dx * dx + dy * dy);
    // Нормаль (перпендикуляр) к отрезку
    const nx = -dy / len;
    const ny = dx / len;
    // Черточка по обе стороны от середины
    const half = length / 2;
    return {
        x1: mid.x - nx * half,
        y1: mid.y - ny * half,
        x2: mid.x + nx * half,
        y2: mid.y + ny * half
    };
}
```

**Назначение:** Рисует черточку (маркер равенства) **перпендикулярно** отрезку.

**Параметры:**
- `p1`, `p2` — концы отрезка
- `t` — позиция на отрезке (0.5 = середина, по умолчанию)
- `length` — длина черточки в пикселях (по умолчанию 8)

**Возвращает:** Объект `{x1, y1, x2, y2}` — координаты начала и конца черточки.

**Использование в SVG:**
```html
{{-- Маркеры равенства AB = BC --}}
<line :x1="tickAB.x1" :y1="tickAB.y1" :x2="tickAB.x2" :y2="tickAB.y2"
    stroke="#3b82f6" stroke-width="2.5"/>
<line :x1="tickBC.x1" :y1="tickBC.y1" :x2="tickBC.x2" :y2="tickBC.y2"
    stroke="#3b82f6" stroke-width="2.5"/>
```

**В JavaScript:**
```javascript
const tickAB = window.equalityTick(A, B, 0.5, 10);
const tickBC = window.equalityTick(B, C, 0.5, 10);
```

**ВАЖНО:** Черточка маркера равенства **ДОЛЖНА быть перпендикулярна** стороне треугольника. НИКОГДА не используйте фиксированные смещения типа `x - 4, y - 5` — это создаёт черточку под произвольным углом вместо перпендикуляра.

### Функция 3.3: doubleEqualityTick() — двойной маркер равенства

```javascript
function doubleEqualityTick(p1, p2, t = 0.5, length = 8, gap = 4) {
    const dx = p2.x - p1.x;
    const dy = p2.y - p1.y;
    const len = Math.sqrt(dx * dx + dy * dy);
    // Единичный вектор вдоль отрезка
    const ux = dx / len;
    const uy = dy / len;
    // Нормаль (перпендикуляр) к отрезку
    const nx = -dy / len;
    const ny = dx / len;
    // Центральная точка
    const mid = { x: p1.x + dx * t, y: p1.y + dy * t };
    const half = length / 2;
    const halfGap = gap / 2;
    // Первая черточка (смещена назад вдоль отрезка)
    const tick1 = {
        x1: mid.x - ux * halfGap - nx * half,
        y1: mid.y - uy * halfGap - ny * half,
        x2: mid.x - ux * halfGap + nx * half,
        y2: mid.y - uy * halfGap + ny * half
    };
    // Вторая черточка (смещена вперёд вдоль отрезка)
    const tick2 = {
        x1: mid.x + ux * halfGap - nx * half,
        y1: mid.y + uy * halfGap - ny * half,
        x2: mid.x + ux * halfGap + nx * half,
        y2: mid.y + uy * halfGap + ny * half
    };
    return { tick1, tick2 };
}
```

**Назначение:** Рисует две параллельные черточки для обозначения второй пары равных отрезков.

**Параметры:**
- `p1`, `p2` — концы отрезка
- `t` — позиция на отрезке (0.5 = середина)
- `length` — длина каждой черточки (по умолчанию 8)
- `gap` — расстояние между черточками (по умолчанию 4)

**Возвращает:** Объект `{ tick1, tick2 }`, где каждый tick содержит `{x1, y1, x2, y2}`.

**Использование в SVG:**
```html
{{-- Двойные черточки для BN = NC --}}
<line :x1="dblTickBN.tick1.x1" :y1="dblTickBN.tick1.y1" :x2="dblTickBN.tick1.x2" :y2="dblTickBN.tick1.y2"
    stroke="#f59e0b" stroke-width="2"/>
<line :x1="dblTickBN.tick2.x1" :y1="dblTickBN.tick2.y1" :x2="dblTickBN.tick2.x2" :y2="dblTickBN.tick2.y2"
    stroke="#f59e0b" stroke-width="2"/>
```

### Правило: Разные пары равных отрезков — разное количество черточек

**КРИТИЧЕСКИ ВАЖНО:** В геометрии равные отрезки обозначаются одинаковым количеством черточек. Если на рисунке есть **несколько пар** равных отрезков, каждая пара должна иметь **своё количество** черточек.

| Пара равных отрезков | Количество черточек | Функция |
|---------------------|---------------------|---------|
| Первая пара (напр. AM = MB) | 1 черточка | `equalityTick()` |
| Вторая пара (напр. BN = NC) | 2 черточки | `doubleEqualityTick()` |
| Третья пара | 3 черточки | (создать по аналогии) |

**Пример (средняя линия треугольника):**
```javascript
// M — середина AB, N — середина BC
const M = window.pointOnLine(A, B, 0.5);
const N = window.pointOnLine(B, C, 0.5);

// Первая пара: AM = MB (одна черточка, синяя)
const tickAM = window.equalityTick(A, M, 0.5, 8);
const tickMB = window.equalityTick(M, B, 0.5, 8);

// Вторая пара: BN = NC (две черточки, янтарная)
const dblTickBN = window.doubleEqualityTick(B, N, 0.5, 8, 4);
const dblTickNC = window.doubleEqualityTick(N, C, 0.5, 8, 4);
```

**НЕПРАВИЛЬНО:** Все 4 отрезка помечены одной черточкой (AM, MB, BN, NC) — это означает, что все они равны между собой, что неверно!

**ПРАВИЛЬНО:** AM = MB помечены одной черточкой, BN = NC помечены двумя черточками — это показывает две разные пары равных отрезков.

### Функция 4: pointOnLine() — точка на отрезке

```javascript
function pointOnLine(p1, p2, t) {
    return {
        x: p1.x + (p2.x - p1.x) * t,
        y: p1.y + (p2.y - p1.y) * t
    };
}
```

**Правила:**
- `t = 0` → точка p1
- `t = 1` → точка p2
- `t = 0.5` → середина отрезка (для медианы)

### Функция 5: labelOnSegment() — подпись длины стороны

```javascript
function labelOnSegment(p1, p2, offset = 15) {
    const mid = { x: (p1.x + p2.x) / 2, y: (p1.y + p2.y) / 2 };
    const dx = p2.x - p1.x;
    const dy = p2.y - p1.y;
    const len = Math.sqrt(dx * dx + dy * dy);
    // Нормаль перпендикулярна отрезку
    const nx = -dy / len;
    const ny = dx / len;
    return {
        x: mid.x + nx * offset,
        y: mid.y + ny * offset
    };
}
```

**Правила:**
- Подпись длины располагается **перпендикулярно** отрезку
- Отступ: **12-18px** от середины отрезка
- Направление отступа выбирается так, чтобы текст не накладывался на фигуру

### Правила для вспомогательных линий

#### Биссектриса
1. Биссектриса **ДОЛЖНА** доходить до противоположной стороны
2. Точка D вычисляется как пересечение биссектрисы со стороной BC
3. Линия: `stroke-dasharray="6,4"`, цвет `#10b981`
4. Точка D: круг радиуса 4px, цвет `#10b981`

```javascript
// Точка D на стороне BC для биссектрисы из A
function bisectorPoint(A, B, C) {
    // Биссектриса делит противоположную сторону в отношении прилежащих сторон
    const AB = Math.sqrt((B.x - A.x)**2 + (B.y - A.y)**2);
    const AC = Math.sqrt((C.x - A.x)**2 + (C.y - A.y)**2);
    const t = AB / (AB + AC);
    return pointOnLine(B, C, t);
}
```

#### Медиана
1. Медиана идёт от вершины до **середины** противоположной стороны
2. Точка M: `M = pointOnLine(A, C, 0.5)`
3. Линия: `stroke-dasharray="6,4"`, цвет `#10b981`
4. Маркеры равенства AM = MC: короткие штрихи на серединах отрезков

**Подписи длин при медиане/высоте:**

Когда медиана (BM) идёт от вершины B к середине основания AC:

| Элемент | Позиционирование | Пример |
|---------|------------------|--------|
| Длина медианы (BM) | Справа от **середины** медианы, отступ **18px** | `x = (B.x + M.x) / 2 + 18`, `y = (B.y + M.y) / 2` |
| Длина основания (AC) | По центру основания, **ниже** линии на **38px** | `x = (A.x + C.x) / 2`, `y = A.y + 38` |

**Важные правила:**
- Позиция X считается от **середины медианы** `(B.x + M.x) / 2`, а не от M.x — это обеспечивает одинаковое расстояние от линии для разных наклонов медианы
- Текст длины основания должен быть значительно **ниже** точки M (38px от основания)
- Показывать только **цифры** без буквенных обозначений (10, а не BM=10)
- Цвет длины медианы: `#10b981` (зелёный, как линия)
- Цвет длины основания: `#f59e0b` (янтарный для выделения)

```html
{{-- Метка длины медианы BM --}}
<text :x="(B.x + M.x) / 2 + 18" :y="(B.y + M.y) / 2"
    fill="#10b981" font-size="13" class="geo-label" text-anchor="middle">10</text>

{{-- Метка длины основания AC --}}
<text :x="(A.x + C.x) / 2" :y="A.y + 38"
    fill="#f59e0b" font-size="13" class="geo-label" text-anchor="middle">14</text>
```

#### Высота
1. Высота **перпендикулярна** основанию
2. Точка H — основание высоты, обязательно на стороне
3. Прямой угол обозначается квадратиком через `rightAnglePath()`

### Правила для текстовых меток

#### Подписи вершин (A, B, C, D, M, H)
```html
<text :x="labelPos(A, center, 24).x" :y="labelPos(A, center, 24).y"
    fill="#60a5fa" font-size="18" class="geo-label"
    text-anchor="middle" dominant-baseline="middle">A</text>
```

#### Метки углов (68°, 42°)
1. Располагать **рядом с дугой**, не на ней
2. Использовать функцию для вычисления позиции метки угла:

```javascript
// bias: 0.5 = точная середина между p1 и p2
//       <0.5 = ближе к p1
//       >0.5 = ближе к p2
function angleLabelPos(vertex, p1, p2, labelRadius, bias = 0.5) {
    const angle1 = Math.atan2(p1.y - vertex.y, p1.x - vertex.x);
    const angle2 = Math.atan2(p2.y - vertex.y, p2.x - vertex.x);

    // Нормализуем разницу углов к диапазону (-π, π]
    let diff = angle2 - angle1;
    while (diff > Math.PI) diff -= 2 * Math.PI;
    while (diff < -Math.PI) diff += 2 * Math.PI;

    // Позиция = angle1 + bias * diff
    const midAngle = angle1 + diff * bias;

    return {
        x: vertex.x + labelRadius * Math.cos(midAngle),
        y: vertex.y + labelRadius * Math.sin(midAngle)
    };
}
```

3. `labelRadius` должен быть **больше** радиуса дуги на 15-20px

4. **ВАЖНО: Углы с биссектрисой** — когда угол делится биссектрисой, метку полного угла НЕЛЬЗЯ размещать через `angleLabelPos(vertex, p1, p2)`, т.к. это поместит её на биссектрису!

   **Неправильно:**
   ```javascript
   // Угол BAC = 68°, AD — биссектриса
   angleLabelPos(A, B, C, 50)  // ❌ Метка окажется НА биссектрисе AD!
   ```

   **Правильно — метка в половине угла BAD:**
   ```javascript
   // D — точка биссектрисы на противоположной стороне BC
   const D = bisectorPoint(A, B, C);

   // Дуга полного угла BAC с радиусом 45px
   makeAngleArc(A, B, C, 45)

   // Метка градусов в половине угла BAD:
   // - labelRadius = 62 (дуга 45 + отступ 17)
   // - bias = 0.6 для визуального центрирования между стороной AB и биссектрисой AD
   angleLabelPos(A, B, D, 62, 0.6)  // ✅ Метка визуально по центру между AB и AD
   ```

   **Почему bias = 0.6, а не 0.5?**
   - Математический центр (0.5) НЕ совпадает с визуальным центром
   - Из-за кривизны дуги, текст при bias=0.5 визуально смещён к стороне треугольника
   - bias=0.6 компенсирует это и даёт визуально центрированную метку

   **Рекомендуемые параметры для биссектрисы:**
   | Параметр | Значение | Комментарий |
   |----------|----------|-------------|
   | Радиус дуги полного угла | 45px | Достаточно места для метки |
   | labelRadius | 62px | = 45 + 17 (отступ от дуги) |
   | bias | 0.6 | Визуальное центрирование |
   | Радиус дуг половинных углов | 30px | Меньше, чтобы не мешать метке |

#### Метки длин сторон (AC = 14, BM = 10)
1. Использовать `labelOnSegment()` для позиционирования
2. Размер шрифта: **11-12px**
3. Цвет: `#94a3b8` для сторон, `#10b981` для вспомогательных линий
4. Текст **НЕ ДОЛЖЕН** пересекать линии

### Правила для прямоугольного треугольника

1. Прямой угол обозначается квадратиком `rightAnglePath(C, A, B, 15)`
2. Вершина прямого угла (`C`) должна быть **первым аргументом**
3. Другие две вершины (`A`, `B`) — это концы катетов
4. Гипотенуза — сторона напротив прямого угла

**Отступы для меток длин (теорема Пифагора):**

| Элемент | Отступ | Пример |
|---------|--------|--------|
| Катеты (известные значения) | **8px** — ближе к стороне | `labelOnSegment(A, B, 8)` |
| Гипотенуза (искомое значение) | **16px** — дальше от стороны | `labelOnSegment(B, C, 16)` |

**Пример:**
```html
{{-- Катеты --}}
<text :x="labelOnSegment(A, C, 8, true).x" :y="labelOnSegment(A, C, 8, true).y"
    fill="#94a3b8" font-size="12">24</text>
<text :x="labelOnSegment(A, B, 8).x" :y="labelOnSegment(A, B, 8).y"
    fill="#94a3b8" font-size="12">7</text>

{{-- Гипотенуза (искомая) --}}
<text :x="labelOnSegment(B, C, 16).x" :y="labelOnSegment(B, C, 16).y"
    fill="#10b981" font-size="12">?</text>
```

### Правила размещения элементов в viewBox

#### 1. Проверка viewBox для вершин у края

**ВАЖНО:** Вершины треугольника не должны быть ближе **35px** к краю viewBox, чтобы `labelPos()` не выводил подписи за пределы видимой области.

**Пример проблемы:**
```javascript
// НЕПРАВИЛЬНО: B слишком близко к верху (y=7)
const B = { x: 150, y: 7 };  // labelPos выведет подпись на y ≈ -15 (за viewBox!)

// ПРАВИЛЬНО: B с отступом от края (y=40)
const B = { x: 150, y: 40 }; // labelPos выведет подпись на y ≈ 18 (видно)
```

#### 2. Проверка viewBox для описанных/вписанных окружностей

При добавлении окружности (описанной или вписанной) нужно убедиться, что она полностью помещается в viewBox:

```
Левая граница:   O.x - R > 5
Правая граница:  O.x + R < viewBox.width - 5
Верхняя граница: O.y - R > 5
Нижняя граница:  O.y + R < viewBox.height - 5
```

**Пример:** viewBox="0 0 300 220", окружность с центром O(150, 120) и R=117:
- Нижняя граница: 120 + 117 = 237 > 220 — **НЕ ПОМЕЩАЕТСЯ!**
- Решение: уменьшить треугольник или увеличить viewBox

#### 3. Внешний угол — позиционирование подписи вершины

Когда от вершины идёт линия внешнего угла (продолжение стороны), стандартный `labelPos()` может разместить подпись поверх этой линии.

**Решение:** Для вершины с внешним углом использовать фиксированное позиционирование:

```html
{{-- НЕПРАВИЛЬНО: labelPos может наложить подпись на линию внешнего угла --}}
<text :x="labelPos(C, center, 22).x" :y="labelPos(C, center, 22).y">C</text>

{{-- ПРАВИЛЬНО: размещаем C ниже точки --}}
<text :x="C.x" :y="C.y + 25">C</text>
```

### Чек-лист перед отправкой SVG

- [ ] Все подписи вершин используют `labelPos()` и не накладываются на фигуру
- [ ] Все дуги углов начинаются и заканчиваются на сторонах (не выходят за пределы)
- [ ] Биссектриса/медиана/высота доходит до противоположной стороны
- [ ] Метки углов не накладываются на дуги
- [ ] **Для биссектрис:** метка угла через `angleLabelPos(A, B, D, 62, 0.6)` — в половине угла с bias=0.6
- [ ] **Для медианы:** метка длины через `(B.x + M.x) / 2 + 18` — от середины линии, не от M.x
- [ ] Метки длин не накладываются на линии
- [ ] Прямой угол обозначен квадратиком в правильной вершине
- [ ] **Теорема Пифагора:** катеты offset=8, гипотенуза offset=16
- [ ] Все вспомогательные точки (D, M, H) расположены на сторонах треугольника
- [ ] Цветовая схема соответствует спецификации

### Пример правильной структуры данных

```javascript
function triangleTask() {
    // Вершины треугольника
    const A = { x: 30, y: 190 };
    const B = { x: 150, y: 30 };
    const C = { x: 270, y: 190 };

    // Центр (центроид)
    const center = {
        x: (A.x + B.x + C.x) / 3,
        y: (A.y + B.y + C.y) / 3
    };

    // Вспомогательные точки
    const M = pointOnLine(A, C, 0.5);  // Середина AC для медианы
    const D = bisectorPoint(A, B, C);   // Точка на BC для биссектрисы из A

    return {
        A, B, C, center, M, D,
        labelPos: (p, c, d) => window.labelPos(p, c, d),
        makeAngleArc: (v, p1, p2, r) => window.makeAngleArc(v, p1, p2, r),
        rightAnglePath: (v, p1, p2, s) => window.rightAnglePath(v, p1, p2, s),
        pointOnLine: (p1, p2, t) => window.pointOnLine(p1, p2, t),
        labelOnSegment: (p1, p2, o) => window.labelOnSegment(p1, p2, o),
        // ВАЖНО: bias параметр обязателен для правильной работы с биссектрисами
        angleLabelPos: (v, p1, p2, r, bias) => window.angleLabelPos(v, p1, p2, r, bias),
    };
}
```

### CSS стили для SVG

```css
.geo-line {
    transition: stroke 0.2s ease, stroke-width 0.2s ease;
}
.geo-point {
    transition: r 0.2s ease, fill 0.2s ease;
}
.geo-label {
    font-family: 'Times New Roman', serif;
    font-style: italic;
    font-weight: 500;
    user-select: none;
    pointer-events: none;
}
```

---

## ВАЖНОЕ ПРАВИЛО: Соответствие PDF-источникам

### Нумерация и структура заданий

**КРИТИЧЕСКИ ВАЖНО:** При создании заданий на основе PDF-файлов:

1. **Номера заданий должны ТОЧНО соответствовать PDF:**
   - Если в PDF задание имеет номер 45 — в коде оно ДОЛЖНО быть номером 45
   - НЕЛЬЗЯ менять номера заданий для "красоты" последовательности

2. **Текст заданий должен соответствовать PDF:**
   - Формулировки, числа и условия — точно как в источнике
   - Ответы — точно как в PDF

3. **Порядок разделов определяется номерами заданий:**
   - Разделы располагаются по возрастанию номеров заданий
   - Если раздел "Площадь" содержит задания 33-36, а "Пифагор" — 45-46,
     то "Площадь" должен идти ПЕРЕД "Пифагором"
   - При необходимости переставляйте РАЗДЕЛЫ, но НЕ меняйте номера заданий

**Пример НЕПРАВИЛЬНО:**
```
Задача 33 (была 45) ← НЕЛЬЗЯ менять номер!
Задача 34 (была 46) ← НЕЛЬЗЯ менять номер!
```

**Пример ПРАВИЛЬНО:**
```
IX) Площадь (задания 33-36)  ← раньше по номерам
X) Пифагор (задания 45-46)   ← позже по номерам
```

### Зачем это важно?

- Учителя используют PDF-источники для проверки
- Ученики сверяют ответы с оригиналом
- Несоответствие номеров создаёт путаницу
- Структура PDF — это стандарт, который нельзя нарушать
